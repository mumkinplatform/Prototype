-- ============================================================
-- Migration 015: track when decision email was sent for a registration
-- ============================================================
-- الغرض:
--   إضافة عمود يسجل وقت إرسال إيميل القبول/الرفض للمشارك.
--   يخدم غرضين:
--     1) منع تكرار إرسال نفس الإيميل لنفس المشارك
--     2) قسم "مراسلة" في صفحة إدارة التسجيلات يقدر يميز:
--        - الطلبات اللي تم إبلاغها (notification_sent_at IS NOT NULL)
--        - الطلبات اللي لسا بانتظار الإرسال (NULL)
--
-- الحقل الجديد:
--   - notification_sent_at DATETIME NULL
--     * NULL = لم يتم إرسال إيميل بعد
--     * timestamp = وقت آخر إيميل تم إرساله بنجاح
--
-- ملاحظات:
--   * كل الصفوف الموجودة بتاخذ القيمة الافتراضية NULL تلقائياً.
--   * لا يتم مسح هذا الحقل عند تغيير حالة الطلب — اعتمدنا على
--     مراجعة المنظم قبل الإرسال (يشوف معاينة القالب ويأكد).
-- ============================================================

USE mumkin_db;

ALTER TABLE `applies_hackathon`
  ADD COLUMN `notification_sent_at`
    DATETIME NULL
    AFTER `reviewed_at`;
