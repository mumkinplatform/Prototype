-- ============================================================
-- Migration 018: drop the session table
-- ============================================================
-- الغرض:
--   تاب "الجلسات" انحذف من واجهة المشارك، وما عاد فيه endpoints
--   تستخدم الجدول. هذي الـ migration تحذف الجدول من قاعدة البيانات
--   تماماً مع ملفات الـ seed و migration القديمة المرافقة لها.
--
-- ملاحظات:
--   * IF EXISTS لجعلها idempotent — تشتغل بدون خطأ حتى لو الجدول
--     غير موجود (مثل ما يحصل في تثبيت جديد بعد حذف 009_session.sql).
-- ============================================================

USE mumkin_db;

DROP TABLE IF EXISTS `session`;
