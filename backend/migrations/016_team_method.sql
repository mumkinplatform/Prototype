-- ============================================================
-- Migration 016: capture team formation intent at registration
-- ============================================================
-- الغرض:
--   لما المشارك يسجل ويختار "فريق"، عنده خياران:
--     1) فريق بالذكاء الاصطناعي (يستخدم Smart Matchmaking)
--     2) فريق يدوي (يدعو أعضاء بالإيميل)
--   نحتاج نخزن اختياره وقت التسجيل عشان المنظم يقدر يصنّف الطلبات
--   ويفلتر القائمة حسب طريقة تكوين الفريق.
--
-- الحقل الجديد:
--   - team_method ENUM('ai', 'manual') NULL
--     * NULL  = المشارك سجّل كفردي (لا ينطبق)
--     * 'ai'  = اختار فريق عبر الذكاء الاصطناعي
--     * 'manual' = اختار فريق يدوي
--
-- ملاحظات:
--   * كل الصفوف الموجودة بتاخذ NULL تلقائياً. التطبيق يضمن أن
--     team_method يتم تعبئته فقط عند participation_type='team'.
-- ============================================================

USE mumkin_db;

ALTER TABLE `applies_hackathon`
  ADD COLUMN `team_method`
    ENUM('ai', 'manual')
    NULL
    AFTER `participation_type`;
